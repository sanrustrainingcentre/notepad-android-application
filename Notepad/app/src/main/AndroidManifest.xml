<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.sanrus.notepad">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <supports-screens android:smallScreens="false"/>

    <!-- Not required to enter sdk level in here, but will do in app/build.gradle file  -->
    <!-- <uses-sdk android:minSdkVersion="15" android:maxSdkVersion="25"/> -->

    <application
        android:name=".NotepadApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.SplashActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity android:name=".activities.DashboardActivity">

            <intent-filter>
                <action android:name="ca.sanrus.internals.dashboard_screen"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>

        </activity>

    </application>

</manifest>
